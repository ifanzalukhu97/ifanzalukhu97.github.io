source "https://rubygems.org"

# Use Jekyll 4.x for local development (compatible with Ruby 4.0)
# GitHub Pages will use its own build environment when deployed
gem "jekyll", "~> 4.3"
gem "webrick" # Required for Jekyll serve on Ruby 3+
gem "csv"
gem "base64"
gem "bigdecimal"
